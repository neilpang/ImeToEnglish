name: Build & Release

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '9.0.x'

      - name: Publish (framework-dependent single-file, win-x64)
        run: dotnet publish ImeToEnglish.csproj -c Release -r win-x64 -o publish

      - name: Rename exe with version
        id: pkg
        shell: pwsh
        run: |
          if ($env:GITHUB_REF -like 'refs/tags/*') {
            $version = $env:GITHUB_REF -replace 'refs/tags/',''
          } else {
            $version = "dev-$(git rev-parse --short HEAD)"
          }
          $name = "ImeToEnglish-$version-win-x64.exe"
          Move-Item publish/ImeToEnglish.exe $name -Force
          echo "asset=$name" >> $env:GITHUB_OUTPUT
          echo "version=$version" >> $env:GITHUB_OUTPUT

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: ImeToEnglish-${{ steps.pkg.outputs.version }}
          path: ${{ steps.pkg.outputs.asset }}
          retention-days: 30

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          files: ${{ steps.pkg.outputs.asset }}
          generate_release_notes: true
          draft: false
          prerelease: false
